class city:
	def __init__(self, label, xpos, ypos):
			self.name = label
			self.x = xpos
			self.y = ypos
			
class route:
	def __init__(self, cityList)
		self.route = cityList
		self.distance = 0.0
		self.fitness = 0.0
	
	def distance(self):
		self.distance = 0
		for i in range(len(self.route)+1):
			
	
class ga:
	def __init__(self, routes)
		self.pop = routes
		
	def rank(self):
		rankings =
		return np.argsort

	def matingPool:
		
	def mate:
		
	def mutate:
		
	def mutatePop:

	def go(self, nGenerations, nPopulation, nElite = 2, Pc = 0.8, Pm = 0.05, 
			verbose = False, plot = False)

	def plotRoute:

	def plotPop:

def bruteForce(cityList):
	route = 
	

	

